---
const currentYear = new Date().getFullYear();

const footerLinks = {
  navegacion: [
    { href: '/catalogo', label: 'Catálogo' },
    { href: '/como-funciona', label: 'Cómo funciona' },
    { href: '/quienes-somos', label: 'Quiénes somos' },
  ],
  cuenta: [
    { href: '/cliente', label: 'Mi cuenta' },
    { href: '/cliente/alquileres', label: 'Mis alquileres' },
    { href: '/cliente/programar', label: 'Programar' },
  ],
  legal: [
    { href: '#', label: 'Política de privacidad' },
    { href: '#', label: 'Términos y condiciones' },
    { href: '#', label: 'Política de cookies' },
  ],
};
---

<footer class="bg-[var(--color-brown-primary)] text-white mt-auto">
  <div class="container py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12">
      <!-- Brand -->
      <div class="lg:col-span-1">
        <div class="flex items-center gap-3 mb-4">
          <img src="/logo-white.svg" alt="Compartirium" class="h-10 w-10" />
          <span class="text-xl font-semibold">Compartirium</span>
        </div>
        <p class="text-white/80 mb-4 italic">"Més que compartir"</p>
        <p class="text-white/70 text-sm">
          Material educativo en alquiler para escuelas y familias. Sostenible, asequible, circular.
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h4 class="font-semibold mb-4">Navegación</h4>
        <ul class="space-y-2">
          {footerLinks.navegacion.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Account -->
      <div>
        <h4 class="font-semibold mb-4">Mi cuenta</h4>
        <ul class="space-y-2">
          {footerLinks.cuenta.map(link => (
            <li>
              <a href={link.href} class="text-white/70 hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Contact -->
      <div>
        <h4 class="font-semibold mb-4">Contacto</h4>
        <ul class="space-y-3 text-white/70">
          <li class="flex items-center gap-2">
            <i class="ph ph-envelope"></i>
            <span>hola@compartirium.es</span>
          </li>
          <li class="flex items-center gap-2">
            <i class="ph ph-phone"></i>
            <span>+34 93 123 45 67</span>
          </li>
          <li class="flex items-start gap-2">
            <i class="ph ph-map-pin mt-1"></i>
            <span>Carrer de l'Educació, 42<br/>08001 Barcelona</span>
          </li>
        </ul>

        <!-- Social -->
        <div class="flex gap-4 mt-6">
          <a href="#" class="text-white/70 hover:text-white transition-colors" aria-label="Instagram">
            <i class="ph ph-instagram-logo text-xl"></i>
          </a>
          <a href="#" class="text-white/70 hover:text-white transition-colors" aria-label="LinkedIn">
            <i class="ph ph-linkedin-logo text-xl"></i>
          </a>
          <a href="#" class="text-white/70 hover:text-white transition-colors" aria-label="Twitter">
            <i class="ph ph-twitter-logo text-xl"></i>
          </a>
        </div>
      </div>
    </div>

    <!-- Bottom -->
    <div class="border-t border-white/20 mt-12 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
      <p class="text-white/60 text-sm">
        © {currentYear} Compartirium. Todos los derechos reservados.
      </p>
      <div class="flex gap-6 text-sm">
        {footerLinks.legal.map(link => (
          <a href={link.href} class="text-white/60 hover:text-white transition-colors">
            {link.label}
          </a>
        ))}
      </div>
    </div>
  </div>
</footer>
